pipeline {
    agent any
    stages {
        stage("Clean Workspace") {
                cleanWs()
        }
        stage("Prepare tools & dupkg") {
                    
        }

        stage("build") {
            
        }

        stage("Upload DKF") {
            
        }
    }
}